# nginx proxy address - http://172.17.0.1:59073

name: bytestash
x-logging: &default-logging
  options:
    max-size: '5m'
    max-file: '1'
  driver: json-file

services:
  app:
    image: ghcr.io/jordan-dalby/bytestash:latest
    container_name: bytestash-app
    hostname: ${BASE_HOST_NAME:-$HOSTNAME}
    restart: always
    pull_policy: always
    logging: *default-logging
    environment:
      TZ: ${TZ:-America/New_York}
      JWT_SECRET: ${APP_SECRET_KEY:-changeme_secret_key_min_32_chars}
      ALLOW_REGISTRATION: ${BYTESTASH_ALLOW_REGISTRATION:-true}
      TOKEN_EXPIRY_HOURS: ${BYTESTASH_TOKEN_EXPIRY_HOURS:-24}
      DISABLE_ACCOUNTS: ${BYTESTASH_DISABLE_ACCOUNTS:-false}
    ports:
      - '172.17.0.1:59073:5000'
    volumes:
      - './volumes/data/bytestash:/data/snippets'
    healthcheck:
      test: ['CMD-SHELL', 'curl -f http://localhost:5000 || exit 1']
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 90s
    networks:
      - bytestash

networks:
  bytestash:
    name: bytestash
    external: false
